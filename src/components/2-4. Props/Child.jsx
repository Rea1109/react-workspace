// eslint-disable-next-line react/prop-types
export const Child = ({ name, age }) => {
    return (
        <div>
            <span>{name}</span>
            <span>{age}</span>
        </div>
    );
};
